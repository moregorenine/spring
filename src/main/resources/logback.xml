<?xml version="1.0" encoding="UTF-8"?>
<configuration>
	<!-- <include resource="org/springframework/boot/logging/logback/base.xml" /> -->

	<!-- Logback을 이용하여 로깅을 수행하기 위해서 필요한 주요 설정요소로는 Logger, Appender, Encoder의 
		3가지가 있으며 각각은 다음과 같은 역할을 수행한다. -->
	<!-- Appender : 로그 메시지가 출력될 대상을 결정하는 요소 -->
	<!-- Appender는 로그를 수행할때 로그 정보를 어디에 기록할지를 결정한다.로그정보를 기록할 수 있는 곳으로는 콘솔, 파일, 
		DB, 메일, JMS등이 있다. -->
	<!-- Logger : 실제 로깅을 수행하는 구성요소로 Level 속성을 통해서 출력할 로그의 레벨을 조절할 수 있다. -->
	<!-- Encoder : Appender에 포함되어 사용자가 지정한 형식으로 표현 될 로그메시지를 변환하는 역할을 담당하는 요소 -->

	<appender name="console" class="ch.qos.logback.core.ConsoleAppender">
		<encoder>
			<!-- <pattern>[%thread] %-5level %logger{35} - %msg %n
			</pattern> -->
			<pattern>%d{HH:mm:ss.SSS} ${PID} [%thread] %30logger [%-5level:] %msg%n
			</pattern>
		</encoder>
	</appender>

	<!-- <appender name="dailyRollingFileAppender"
		class="ch.qos.logback.core.rolling.RollingFileAppender">
		prudent : 다수의 JVM에 하나의 파일에 로그를 안전하게 남길수 있도록 동기화를 할지 여부를 결정하는 속성
		<prudent>true</prudent>
		<file>currentLog.log</file>

		rolling이 발생할 때 어떤 작업을 수행할지 결정하는 속성
		<rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
			<fileNamePattern>moregore.%d{yyyy-MM-dd}.log</fileNamePattern>
			<maxHistory>90</maxHistory>
		</rollingPolicy>

		어떤 조건에서 rolling 이벤트가 발생할 지 조건을 결정하는 속성
		<triggeringPolicy
			class="ch.qos.logback.core.rolling.SizeBasedTriggeringPolicy">
			<maxFileSize>5MB</maxFileSize>
			5MB 파일크기로 Rolling을 수행
		</triggeringPolicy>

		<filter class="ch.qos.logback.classic.filter.ThresholdFilter">
			<level>INFO</level>
		</filter>

		<encoder>
			<pattern>%d{HH:mm:ss.SSS} %-5level -[%thread] %logger{35} : %msg%n
			</pattern>
		</encoder>
	</appender> -->

	<!-- <logger name="org.springframework.web" level="INFO" />
	<logger name="org.thymeleaf" level="INFO" />
	<logger name="org.hibernate.SQL" level="INFO" />
	<logger name="org.quartz.core" level="INFO" />
	<logger name="org.h2.server.web" level="INFO" /> -->
	
	<!-- <logger name="org.springframework.web" level="INFO" /> -->
	<logger name="com.moregore" level="DEBUG"></logger>

	<root level="INFO">
		<appender-ref ref="console" />
	</root>
</configuration>